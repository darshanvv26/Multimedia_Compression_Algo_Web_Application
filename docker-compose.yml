services:
  backend:
    build: ./backend
    container_name: compression_backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: compression_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
